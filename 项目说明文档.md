# 飞书消费记账助手 · 项目说明文档

## 一、项目概述

一个轻量级的银行消费短信转发工具。用户通过网页输入银行短信（或手动填写字段），系统自动解析为结构化数据，**同时**完成两件事：

1. **发送富文本消息到飞书群机器人**（即时通知）
2. **写入飞书多维表格**（长期记录 + 看板统计）

```
用户浏览器 (http://localhost:8088)
        │
        ▼
  Spring Boot 后端
    ├── ① 短信解析（正则提取字段）
    ├── ② 飞书群消息（自定义机器人 webhook）
    └── ③ 多维表格写入（开放平台 API + App 认证）
```

---

## 二、技术栈

| 层 | 技术 | 版本 | 用途 |
|----|------|------|------|
| 后端框架 | Spring Boot | 3.2.10 | Web 服务、依赖注入、静态资源托管 |
| JDK | Java | 17 | 编译运行 |
| HTTP 工具 | Hutool | 5.8.31 | 调飞书 API（HttpRequest + JSON） |
| 前端框架 | Vue 3 | CDN | 响应式数据绑定 |
| 前端 UI | Vant 4 | CDN | 移动端组件库 |
| 构建工具 | Maven | — | 依赖管理、编译打包 |
| 飞书集成 | 自定义机器人 + 开放平台 API | — | 群消息 + 多维表格 |

> 前端采用 CDN 方式引入 Vue 和 Vant，**无需 npm 构建**，index.html 直接由 Spring Boot 作为静态资源提供。

---

## 三、项目结构

```
feishu-project/
├── pom.xml                              # Maven 依赖配置
├── 项目说明文档.md                       # 本文档
│
├── src/main/resources/
│   ├── application.properties           # 配置文件（端口、webhook、飞书应用凭证）
│   └── static/
│       └── index.html                   # 前端页面（Vue3 + Vant，CDN 引入）
│
└── src/main/java/com/code/feishu/
    ├── FeishuSpringApplication.java     # 启动类
    │
    ├── controller/
    │   └── MessageController.java       # HTTP 接口（/api/parse、/api/send、/api/ping）
    │
    ├── service/
    │   ├── MessageParserService.java    # 短信解析（正则 → 结构化字段）
    │   ├── FeishuBotService.java        # 飞书群机器人消息发送
    │   └── FeishuBitableService.java    # 飞书多维表格记录写入
    │
    ├── dto/
    │   └── MessageSendDTO.java          # 前端 → 后端的请求体
    │
    ├── vo/
    │   └── MessageParseVO.java          # 解析结果返回给前端
    │
    └── pojo/                            # 早期遗留的空类（未使用，可清理）
        ├── meassageDto.java
        └── messageVo.java
```

---

## 四、开发流程（从头到尾）

### 阶段 1：修复飞书群机器人（让它能发消息）

**初始问题：**
- `MeassageWebhook.java` 类没有加 `@Component`，Spring 不托管，`@PostConstruct` 永远不执行
- 请求体直接发裸字符串 `"哈哈"`，不符合飞书 webhook 协议（要求 JSON）

**修复方案：**
- 重写为 `FeishuBotService.java`，加 `@Service` 注解
- 使用飞书 `post` 富文本消息格式（比纯 text 更美观，PC/手机都好看）

**关键坑点 —— `text` vs `content` 字段名：**

飞书 post 富文本中，每个文本元素的字段名是 **`text`**，不是 `content`：
```json
// ✅ 正确
{"tag": "text", "text": "银行：工商银行"}

// ❌ 错误（飞书会返回 code:10002 "text is nil"）
{"tag": "text", "content": "银行：工商银行"}
```

### 阶段 2：开发前端页面

用 Vue3 + Vant（CDN）实现单页面，放在 `static/index.html`，Spring Boot 自动托管。

**两种输入方式：**
- **方式一**：粘贴完整银行短信 → 点"解析"自动填充到下方字段
- **方式二**：手动逐项填写（银行、尾号、时间、收支、渠道、商家、金额、余额）

**关键坑点 —— IDEA 内置服务器 vs Spring Boot 同源：**

从 IDEA 右键打开 HTML 会走 IDEA 内置静态服务器（端口 63342），导致 `fetch('/api/xxx')` 请求到 63342 而非后端的 8088，返回 404。

**解决：** 必须用浏览器直接访问 `http://localhost:8088/`，让页面和 API 同源。

### 阶段 3：接入飞书多维表格

**核心区别：**
- 群机器人：只需一个 webhook URL，无需认证
- 多维表格 API：需要飞书自建应用认证（App ID + Secret → tenant_access_token）

**配置步骤（飞书端）：**
1. 在 [open.feishu.cn](https://open.feishu.cn) 创建企业自建应用 → 获取 App ID / App Secret
2. 权限管理 → 开通 `bitable:app`（多维表格读写权限）
3. 发布版本（管理员审批）
4. 多维表格 URL 里提取 app_token（`/base/` 后面那段）
5. 通过 API 获取真正的 table_id（URL 里的 `table=` 参数是视图 ID，不是表 ID）
6. 在多维表格里「添加文档应用」→ 授权给自建应用

**关键坑点 —— table_id 不在 URL 里：**

飞书多维表格 URL 格式：
```
https://xxx.feishu.cn/base/{app_token}?table={视图ID不是table_id}
```

真正的 table_id 需要通过 API 获取：
```
GET /open-apis/bitable/v1/apps/{app_token}/tables
→ 返回 {"items":[{"table_id":"tblWlKHekiLMyF2N","name":"收支记录表"}]}
```

### 阶段 4：后端整合（群消息 + 表格同时执行）

修改 `/api/send` 接口，一次请求同时完成两件事：
1. 调 `FeishuBotService.send()` 发群消息
2. 调 `FeishuBitableService.writeRecord()` 写表格

返回两个独立结果，前端分别展示状态。

---

## 五、后端详细说明

### 5.1 配置文件 `application.properties`

```properties
# 服务端口
server.port=8088

# ===== 飞书自定义机器人（群消息） =====
# 在飞书群 → 设置 → 群机器人 → 添加自定义机器人后获取
feishu.bot.webhook=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx

# ===== 飞书开放平台自建应用（多维表格 API） =====
# 在 open.feishu.cn 开发者后台创建自建应用后获取
feishu.app.id=cli_xxxxxxxxx
feishu.app.secret=xxxxxxxxxxxxxxxxxxxxxxxx
# 多维表格 URL 里 /base/ 后面那段
feishu.bitable.app-token=xxxxxxxxxxxxxxxx
# 通过 API 获取的数据表 ID（不是 URL 里的 table= 参数）
feishu.bitable.table-id=tblxxxxxxxx

# 字符集
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true
```

### 5.2 短信解析 `MessageParserService.java`

**职责：** 把银行短信原文用正则解析为 8 个结构化字段。

**解析逻辑（按顺序）：**

| 字段 | 正则 | 样例匹配 |
|------|------|----------|
| 卡尾号 | `尾号\s*(\d{4})` | `尾号6566` → `6566` |
| 日期时间 | `(\d{1,2})月(\d{1,2})日\s*(\d{1,2}):(\d{2})` | `8月1日19:48` → `2026-08-01 19:48`（缺年份自动取当年） |
| 收支方向 | `(支出\|消费\|扣除\|...)` | `支出` → 归一化为 `支出` / `收入` |
| 渠道 | `(支付宝\|微信\|抖音支付\|...)` | `消费抖音支付` → `抖音支付` |
| 商家 | 括号内 `渠道-商家` 结构拆分 | `(消费抖音支付-上海牛约堡...)` → `上海牛约堡...` |
| 金额 | `([收支出取入...].*?)(\d+元)` | `支出9.89元` → `9.89`（自动去掉千分位） |
| 余额 | `余额\s*(\d+元)` | `余额1,247.81元` → `1247.81` |
| 银行 | `【([^】]+)】` | `【工商银行】` → `工商银行` |

**方向归一化映射：**
```
支出 ← 支出、消费、扣除、扣费、扣款、取款、取现、转出
收入 ← 收入、存入、转入、入账、退款、到账、代发、工资
```

**解析失败处理：** 未命中的字段不报错，在 `tip` 里提示"部分字段未命中：xxx，可手动补充"，前端允许手动补充后再发送。

### 5.3 飞书群消息发送 `FeishuBotService.java`

**职责：** 把结构化字段拼成飞书 `post` 富文本消息，通过自定义机器人 webhook 发送。

**消息格式：**
```json
{
  "msg_type": "post",
  "content": {
    "post": {
      "zh_cn": {
        "title": "[支出]¥9.89 上海牛约堡餐饮集团有限公司",
        "content": [
          [{"tag": "text", "text": "🏦 银行：工商银行"}],
          [{"tag": "text", "text": "💳 卡号：尾号 6566"}],
          [{"tag": "text", "text": "🕒 时间：2026-08-01 19:48"}],
          [{"tag": "text", "text": "💰 金额：支出 ¥9.89"}],
          [{"tag": "text", "text": "🧾 商家：上海牛约堡餐饮集团有限公司"}],
          [{"tag": "text", "text": "🛒 渠道：抖音支付"}],
          [{"tag": "text", "text": "💵 余额：¥1247.81"}],
          [{"tag": "text", "text": "📨 原文：尾号6566卡..."}]
        ]
      }
    }
  }
}
```

**字段优先级：** 如果同时传了 `rawMessage`（完整短信）和拆分字段，优先用解析结果，解析不到的再用前端传的字段补充。

### 5.4 飞书多维表格写入 `FeishuBitableService.java`

**职责：** 用飞书开放平台 API 把消费记录写入多维表格。

**流程：**

```
┌──────────────────────────────────────────────────┐
│  1. 获取 tenant_access_token                     │
│     POST /open-apis/auth/v3/tenant_access_token  │
│     Body: { app_id, app_secret }                 │
│     → 缓存 token（有效期 7200 秒，提前 5 分钟刷新） │
├──────────────────────────────────────────────────┤
│  2. 写入记录                                     │
│     POST /open-apis/bitable/v1/apps/{app_token}  │
│          /tables/{table_id}/records              │
│     Header: Authorization: Bearer {token}        │
│     Body: { fields: { ... } }                    │
└──────────────────────────────────────────────────┘
```

**字段映射（后端解析字段 → 多维表格列）：**

| 多维表格列名 | 类型 | 数据来源 | 备注 |
|-------------|------|----------|------|
| 收支类型 | 单选 | direction | 值为"支出"或"收入" |
| 分类 | 单选 | **自动推断** | 根据商家名/渠道关键词匹配 |
| 金额 | 数字 | amount | 传 double |
| 时间 | 日期时间 | happenTime | 转毫秒时间戳 |
| 备注 | 文本 | 商家+渠道+银行+尾号+余额+原文 | 拼接成一段文本 |
| 记录编号 | 自动编号 | — | 系统自动生成，不可写 |
| 结余金额 | 公式 | — | 表格内公式自动计算，不可写 |

**分类自动推断逻辑：**

```java
商家名/渠道包含以下关键词 → 自动归类：
  餐饮：餐、食、饭、面、堡、饮、茶、咖啡、外卖、美团、麦当劳...
  交通：打车、地铁、公交、滴滴、高铁、机票、停车、加油...
  购物：购物、商城、京东、淘宝、拼多多、超市、便利店...
  住房：房、租、物业、水电、燃气、宽带...
  其他：以上都不匹配时默认
```

### 5.5 API 接口 `MessageController.java`

#### `GET /api/ping` — 健康检查

```json
// Response
{ "ok": true, "ts": 1785597369375 }
```

#### `POST /api/parse` — 解析短信（不发送）

```json
// Request
{ "rawMessage": "尾号6566卡8月1日19:48支出(消费抖音支付-上海牛约堡...)9.89元，余额1,247.81元。【工商银行】" }

// Response
{
  "success": true,
  "tip": "解析成功，确认无误后可直接发送",
  "cardTail": "6566",
  "happenTime": "2026-08-01 19:48",
  "direction": "支出",
  "channel": "抖音支付",
  "merchant": "上海牛约堡餐饮集团有限公司",
  "amount": 9.89,
  "balance": 1247.81,
  "bank": "工商银行",
  "transType": "支出"
}
```

#### `POST /api/send` — 发送消息（群消息 + 写表格）

```json
// Request —— 模式1：传完整短信
{ "rawMessage": "尾号6566卡8月1日19:48支出..." }

// Request —— 模式2：传拆分字段
{
  "bank": "工商银行",
  "cardTail": "6566",
  "happenTime": "2026-08-01 19:48",
  "direction": "支出",
  "channel": "抖音支付",
  "merchant": "上海牛约堡餐饮集团有限公司",
  "amount": 9.89,
  "balance": 1247.81
}

// Response
{
  "ok": true,
  "feishu": "{\"StatusCode\":0,\"StatusMessage\":\"success\"}",   // 群消息结果
  "bitable": "{\"code\":0,\"msg\":\"success\"}"                   // 多维表格结果
}
```

### 5.6 数据对象

#### `MessageSendDTO`（前端 → 后端）

| 字段 | 类型 | 说明 |
|------|------|------|
| rawMessage | String | 完整短信原文（模式1） |
| bank | String | 银行名称 |
| cardTail | String | 卡尾号 |
| happenTime | String | 时间（yyyy-MM-dd HH:mm） |
| direction | String | 收支方向（支出/收入） |
| channel | String | 支付渠道 |
| merchant | String | 商家名称 |
| amount | BigDecimal | 金额 |
| balance | BigDecimal | 余额 |
| transType | String | 交易类型（和 direction 重复，允许前端覆盖） |

#### `MessageParseVO`（解析结果 → 前端）

| 字段 | 类型 | 说明 |
|------|------|------|
| success | Boolean | 是否解析成功 |
| tip | String | 提示信息 |
| cardTail ~ bank | 同 DTO | 解析出的字段 |

---

## 六、前端简要说明

### 技术方案

- **Vue 3 + Vant 4**，通过 CDN 引入，无需 npm/webpack 构建
- 单文件 `index.html`，放在 `src/main/resources/static/`，Spring Boot 自动作为静态资源提供
- 同源访问 `http://localhost:8088/`，无跨域问题

### 页面布局

```
┌─────────────────────────────────┐
│  💬 飞书消息转发助手              │
├─────────────────────────────────┤
│  方式一 · 粘贴完整消息            │
│  ┌─────────────────────────┐    │
│  │  [8行文本框]              │    │
│  └─────────────────────────┘    │
│  [填入支出样例][填入收入样例]      │
│  [🔍 解析到下方字段]             │
├─────────────────────────────────┤
│  方式二 · 逐项填写               │
│  银行: [    ]  尾号: [    ]     │
│  时间: [          ] [🕒]        │
│  收支: [支出 ▼]  渠道: [    ]   │
│  商家: [                    ]   │
│  金额: [    ]  余额: [    ]     │
├─────────────────────────────────┤
│  [状态栏: ✅ 群消息 | ✅ 表格]   │
│  [重置] [📤 发送到飞书]          │
└─────────────────────────────────┘
```

### 交互流程

1. 用户粘贴银行短信 → 点"解析" → 前端调 `/api/parse` → 字段自动回填
2. 用户确认/修改字段 → 点"发送到飞书" → 前端调 `/api/send`
3. 后端同时发群消息 + 写表格 → 返回两个结果
4. 底部状态栏显示：`✅ 群消息已发送 | ✅ 表格已写入`

---

## 七、飞书平台配置

### 7.1 自定义机器人（群消息）

1. 打开飞书群 → 设置 → 群机器人 → 添加机器人 → 选「自定义机器人」
2. 复制 webhook URL（`https://open.feishu.cn/open-apis/bot/v2/hook/xxx`）
3. 粘贴到 `application.properties` 的 `feishu.bot.webhook`

> **安全设置：** 如果开启了「自定义关键词」，发送的消息内容必须包含该关键词；如果开启了「签名校验」，需要额外在代码里计算 sign 字段（本项目暂未实现签名校验）。

### 7.2 自建应用（多维表格 API）

1. 打开 [open.feishu.cn](https://open.feishu.cn) → 开发者后台 → 创建企业自建应用
2. 「凭证与基础信息」→ 获取 **App ID** 和 **App Secret**
3. 「权限管理」→ 搜索「多维表格」→ 开通 `bitable:app` 权限
4. 「版本管理与发布」→ 创建版本 → 申请发布（管理员审批通过后生效）
5. 多维表格右上角「...」→「添加文档应用」→ 搜索应用名 → 添加（权限选「可编辑」）

### 7.3 多维表格配置

本项目使用的「收支记录表」包含以下字段：

| 字段名 | 类型 | 选项/格式 | 写入方式 |
|--------|------|-----------|----------|
| 记录编号 | 自动编号 | REC-{序号} | 系统自动 |
| 收支类型 | 单选 | 收入 / 支出 | API 写入 |
| 分类 | 单选 | 餐饮/交通/购物/住房/其他 | API 写入（自动推断） |
| 金额 | 数字 | 0.00 | API 写入 |
| 时间 | 日期时间 | yyyy/MM/dd | API 写入 |
| 备注 | 文本 | — | API 写入 |
| 结余金额 | 公式 | 自动计算收支净值 | 不可写 |

---

## 八、部署运行

### 环境要求

- JDK 17+
- Maven 3.6+
- 飞书账号（能创建群机器人和自建应用）

### 启动

```bash
# 编译
mvn clean compile

# 启动
mvn spring-boot:run
```

### 访问

- **PC 浏览器：** `http://localhost:8088/`
- **手机浏览器（同局域网）：** `http://电脑IP:8088/`
- ⚠️ 不要从 IDEA 右键打开 HTML（会走 63342 端口，API 请求 404）

### 验证

1. 访问 `http://localhost:8088/api/ping` → 返回 `{"ok":true}` 说明后端启动成功
2. 页面点"填入支出样例"→"解析到下方字段" → 字段自动回填说明解析正常
3. 点"发送到飞书" → 飞书群收到消息 + 多维表格多一行记录 → 全链路通

---

## 九、踩坑记录

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 飞书返回 `text is nil` | post 富文本元素用了 `content` 字段名 | 改为 `text`（飞书规定） |
| 前端请求 404 | 从 IDEA 打开 HTML 走 63342 端口 | 用浏览器直接访问 `localhost:8088` |
| 多维表格 `WrongTableId` | URL 里的 `table=` 是视图 ID 不是表 ID | 调 API 获取真正的 table_id |
| 多维表格 `WrongRequestBody` | 字段名和表格实际列名不匹配 | 调 API 获取字段列表，按实际列名写入 |
| 飞书返回 `frequency limited` | 短时间请求过多触发限流 | 间隔几秒重试 |
